// ============================================================
//  If / Else Statements -- The Syntax
//  Source: Day 1, Slide 65 ("How to Use an 'If-Statement'")
//  NOTE: the slide shows this as a bare syntax fragment -- a
//  setup()/loop() wrapper and a real "someCondition" value were
//  added so it compiles and runs.
// ============================================================
void setup() {

}

void loop() {
  bool someCondition = true;

  if (someCondition) {
    // do stuff if the condition is true
  } else {
    // do stuff if the condition is false
  }
}
